{
  "name": "signature_recognition_app",
  "version": "1.0.0",
  "description": "const express = require('express'); const path = require('path'); const canvas = require('canvas');",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.11.2",
    "express": "^4.18.2"
  }
}
